---
  - name: Preenchendo Inventario
    hosts: localhost
    tasks:

    - name: Preenchendo Inventario
      delegate_to: localhost
      script:
         cmd: /home/inventory_creator/windows_inventory_creator.sh
